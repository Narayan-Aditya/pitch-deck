# Copy to .env.local for local development. On Vercel, set these under
# Project Settings -> Environment Variables instead — .env.local is gitignored
# and never reaches the deploy.

# ---------------------------------------------------------------------------
# REQUIRED
# ---------------------------------------------------------------------------

# Writes the brand summary, the Instagram insight, the audience-fit paragraph,
# and picks which of the creator's videos go on the match slide. Without it
# every "auto-fills by itself" step on the report page fails and has to be
# typed in by hand. https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# Supabase: Google sign-in, report storage, and the usage counters.
# Supabase dashboard -> Project Settings -> API.
#
# The URL is the API endpoint (https://<ref>.supabase.co), NOT the
# https://supabase.com/dashboard/project/<ref> address you browse to. Those are
# different hosts, and the dashboard one answers every query with HTML.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Public by design and safe in the browser bundle: it carries no privileges of
# its own, and row level security decides what each signed-in user may read.
# The service_role key is a different thing entirely — it bypasses RLS, so it
# must never appear here or anywhere else client-side.
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# browse2api API key. This is what fetches a prospect's follower and
# engagement numbers — automatically the first time a report is opened, and
# again whenever "↻ Get latest" is pressed. See
# browse2api-Instagram-API-Reference.pdf at the repo root for the API this
# calls (lib/instagramScrape.js).
#
# SET THIS ON VERCEL TOO. Without it every report opens with zeros and the
# numbers have to be typed in by hand, with no error beyond a note on the
# card. Get a key at skills.browse2api.com.
BROWSE2API_KEY=

# ---------------------------------------------------------------------------
# OPTIONAL
# ---------------------------------------------------------------------------

# Model behind all four generation steps. Defaults to gpt-4.1-mini.
# OPENAI_RESEARCH_MODEL=gpt-4.1-mini

# Supabase service_role key. SERVER ONLY — it bypasses row level security, and
# the audit cache and lookup ledger have RLS on with no policies precisely so
# that this is the only key that can reach them. No NEXT_PUBLIC_ prefix, so Next
# cannot inline it into client output even by accident.
#   Dashboard -> Project Settings -> Data API -> service_role
# Leave it unset and everything still works — just uncached and uncapped.
SUPABASE_SERVICE_KEY=your_service_role_key_here

# How long a cached Instagram lookup may answer for. Raise it to spend less on
# browse2api, lower it for fresher follower counts. 0 disables expiry.
AUDIT_CACHE_TTL_DAYS=7

# Paid Instagram lookups each person gets per calendar month. Cache hits are
# free and never counted against it. 0 removes the cap.
INSTAGRAM_MONTHLY_LIMIT=20
